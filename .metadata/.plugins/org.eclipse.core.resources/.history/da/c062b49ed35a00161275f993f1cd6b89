package com.javacodegeeks.drools;
global String topicLevel
rule "Drools Introduction"
//when
//$droolsIntro : DroolsIntroduction( topic == "Drools" )
//then
//System.out.println($droolsIntro.addition() + ", topic level is " + getDefaultIfNull(topicLevel)) ;
//end
//function String getDefaultIfNull(String topicLevel) {
//return topicLevel == null ? "Moderate" : topicLevel;

when
$droolsintro : DroolsIntroduction( topic == "Drools" )
then
System.out.println($droolsintro.addition() + "equal to a hundred");
when
$droolsintro : DroolsIntroduction( $droolsintro == 100 )
then
System.out.println(hacked);
end
function String getDefaultIfNull(String topicLevel) {
return topicLevel == null ? "Moderate" : topicLevel;
}